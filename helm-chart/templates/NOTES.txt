This Helm chart is meant to be installed in the same namespace as another Helm
chart that relies on an ACME server, and if you see this, perhaps you have
installed it by itself in its own namespace? That isn't how its meant to be.
